version: '3.8'

services:
  web:
    build:
       context: ./
       dockerfile: prod.dockerfile
    expose:
      - 8080
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.fastapi.rule=Host(`pydoorn.nl`)"
      - "traefik.http.routers.fastapi.tls=true"
      - "traefik.http.routers.fastapi.tls.certresolver=letsencrypt"
    volumes:
      - ./root/web/peters_prijslijst_automation/output_volume:/code/output/
      - ./root/web/peters_prijslijst_automation/output_volume2:./output/
      - ./root/web/peters_prijslijst_automation/output_volume3:app/output/

  traefik:
    image: traefik:v2.2
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
      - "$PWD/services/traefik.prod.toml:/etc/traefik/traefik.toml"
